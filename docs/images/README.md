# Screenshot Storage Note

This directory is intentionally kept empty in git except for `.gitkeep`.

Do **not** commit binary screenshots (`.png`, `.jpg`, `.gif`, `.pdf`) to the repository.
Store release/demo screenshots outside git (for example, thesis assets storage or release attachments).
